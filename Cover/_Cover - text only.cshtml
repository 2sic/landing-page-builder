@inherits ToSic.Sxc.Dnn.RazorComponent
@using ToSic.Razor.Blade;
@{ 
    var helpers = CreateInstance("../shared/_Helpers.cshtml");
    var coverHelpers = CreateInstance("_Helpers - Cover.cshtml");
}

<section class='app-landingpage-builder app-lp-cover @helpers.SectionClasses()'
    @helpers.SectionStyle()
    @Edit.TagToolbar(Header, toolbar: "settings&hover=left&autoAddMore=start&class=coverTagbar")
    >
    @helpers.BackgroundDiv()
    <div class="container-fluid d-flex align-items-center">
        @RenderPage("../shared/_Block.cshtml")
        <div class="row justify-content-center w-100 mt-5">            
            <div class="col-12 col-md-8 text-center mb-4" @Edit.TagToolbar(Content) @Edit.Attribute("quick-edit-config", new { modules = new { enable = true }})>
                @if (Text.Has(Content.Icon)) {
                    <i class="content-icon fas @Content.Icon" aria-hidden="true"></i>
                }
                @coverHelpers.Body()
                @helpers.CtaList(Content.CTA)
            </div>
        </div>
        @helpers.HeaderCta(Header.CTA)
    </div>
</section>

@RenderPage("../shared/_Assets.cshtml")
